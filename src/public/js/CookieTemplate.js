// Replace hardcoded defaults with dynamically
// generated data from geolocation etc.

let newCookieTemplate = {
        userOptions: {
            currency: 'gbp',
            language: 'en',
        }
    };

export default newCookieTemplate;