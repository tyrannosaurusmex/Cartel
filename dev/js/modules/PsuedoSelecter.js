export default class PsuedoSelecter {
    constructor(options) {
        this.selecter = options.selecter;
        this.selecterDOM = document.getElementById(this.selecter);
        this.input = otions.input;
        this.inputDOM = document.getElementById(this.input);
        this.selected = this.selected;
    }
}